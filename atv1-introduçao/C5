#include <stdio.h>
 
int main() {
 
    int R;
    double PI = 3.14159, VOLUME;
    scanf("%d", &R);
    VOLUME = (4/3.0) * PI * pow(R, 3);
    printf("VOLUME = %.3lf\n", VOLUME);
    
    return 0;
}
